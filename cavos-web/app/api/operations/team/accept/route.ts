import{createHash}from'node:crypto';import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';import{createAdminClient}from'@/lib/supabase/admin';export async function POST(request:Request){const{token}=await request.json();const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user||!token)return NextResponse.json({error:'Authentication and token are required'},{status:401});const admin=createAdminClient();const{data:invite}=await admin.from('organization_invitations').select('*').eq('token_hash',createHash('sha256').update(token).digest('hex')).is('accepted_at',null).gt('expires_at',new Date().toISOString()).maybeSingle();if(!invite||user.email?.toLowerCase()!==invite.email.toLowerCase())return NextResponse.json({error:'Invitation is invalid, expired, or belongs to another email'},{status:403});await admin.from('organization_members').upsert({organization_id:invite.organization_id,user_id:user.id,role:invite.role});await admin.from('organization_invitations').update({accepted_at:new Date().toISOString()}).eq('id',invite.id);await admin.from('audit_events').insert({organization_id:invite.organization_id,actor_id:user.id,action:'member.joined',resource_type:'membership',resource_id:user.id,result:'success',changes:{role:invite.role}});return NextResponse.json({success:true})}
