import Link from'next/link';import{PageHeader}from'@/components/ui/PageHeader';export default async function DevicesPage({params}:{params:Promise<{id:string}>}){const{id}=await params;return <div className="space-y-6"><PageHeader title="Devices & recovery" subtitle="Device state is shown per wallet to preserve support context."/><div className="rounded-xl border border-line bg-white p-6"><h2 className="text-sm font-semibold">Find a wallet</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-black/50">Open a registered wallet to inspect authorized device labels, pending device additions and recovery readiness. Public-key coordinates are never displayed.</p><Link href={`/dashboard/apps/${id}/wallets`} className="mt-5 inline-flex rounded-lg bg-ink px-4 py-2.5 text-sm font-semibold text-white focus-visible:outline-2 focus-visible:outline-brand">Browse wallets</Link></div></div>}
