'use client';import{Suspense,useEffect,useState}from'react';import{useRouter,useSearchParams}from'next/navigation';export default function AcceptInvitationPage(){return <Suspense fallback={<InvitationCard text="Loading invitation…"/>}><AcceptInvitation/></Suspense>}function AcceptInvitation(){const params=useSearchParams(),router=useRouter();const[state,setState]=useState('Accepting invitation…');useEffect(()=>{const token=params.get('token');if(!token){setState('Invitation token is missing.');return}fetch('/api/operations/team/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error);setState('Invitation accepted. Redirecting…');setTimeout(()=>router.push('/dashboard'),800)}).catch(e=>setState(e.message))},[params,router]);return <InvitationCard text={state}/>}function InvitationCard({text}:{text:string}){return <div className="mx-auto max-w-md rounded-xl border border-line bg-white p-6 text-center"><h1 className="text-lg font-semibold">Join organization</h1><p role="status" className="mt-3 text-sm text-black/55">{text}</p></div>}
