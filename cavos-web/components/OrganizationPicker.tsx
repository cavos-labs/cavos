export function OrganizationPicker({items,value,onChange}:{items:any[];value:string;onChange:(value:string)=>void}){return <label className="flex items-center gap-2 text-xs font-semibold text-black/55">Organization<select value={value} onChange={e=>onChange(e.target.value)} className="rounded-lg border border-line-strong bg-white px-3 py-2 text-sm text-ink focus-visible:outline-2 focus-visible:outline-brand">{items.map(org=><option key={org.id} value={org.id}>{org.name}</option>)}</select></label>}
